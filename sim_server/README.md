# Deprecated

`sim_server/` is the old local WebSocket-oriented SUMO runner.

The active execution-node implementation is now under:

- [worker-agent](/E:/SimFox/worker-agent/README.md)

Keep this folder only as short-term reference while the remaining scheduling path is migrated.
